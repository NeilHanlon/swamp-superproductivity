# BEGIN swamp managed section - DO NOT EDIT

# Runtime data (not needed in version control)
.swamp/

# Local extension sources (developer-specific, not shared)
.swamp-sources.yaml

# Claude Code configuration (managed by swamp)
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
# END swamp managed section

# Internal working notes — keep out of the public repo. Only the published design
# doc (referenced by manifest.yaml additionalFiles) is tracked; everything else
# under docs/ is session scratch (carries local paths / ids) and stays local.
docs/*
!docs/super-productivity-plan.md
